package com.joelbalmes.crud_app;

import androidx.lifecycle.LiveData;
import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;
import androidx.room.Update;

import java.util.List;

@Dao
public interface RecordDao {

//  @Query( "SELECT * FROM records" )
//  List<Record> getAll();
//
//  @Query( "SELECT name FROM records")
//  LiveData<List<String>> getAllNames();
//
//  @Query( "SELECT * FROM records WHERE recordID=:recordID LIMIT 1")
//  LiveData<Record> getByID(int recordID );
//
//  @Query( "SELECT * FROM records WHERE name=:recordName" )
//  LiveData<Record> getByName(String recordName );
//
//  @Insert
//  void addRecord( Record record );
//
//  @Update
//  void updateRecord( Record record );
//
//  @Delete
//  void deleteRecord( Record record );

}
